[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["station.plugin.*"]
namespaces = true

[project]
name = "station-plugin1"
version = "1.0.1"
description = "Station plugin : plugin 1"
license = { text = "Station Corp Software License Agreement" }
