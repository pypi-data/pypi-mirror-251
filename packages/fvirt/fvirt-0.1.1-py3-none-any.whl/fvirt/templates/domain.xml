{# vim: set ft=jinja : #}
{# Actual templates are in domain/, the correct one will be used based
   on the type property of the Domain model passed to the templating
   function.
#}
{% include sub_template %}
