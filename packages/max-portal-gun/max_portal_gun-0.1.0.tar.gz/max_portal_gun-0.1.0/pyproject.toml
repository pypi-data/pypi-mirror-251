[tool.poetry]
name = "max-portal-gun"
version = "0.1.0"
description = ""
authors = ["Max Hartke <hartke@cs.wisc.edu>"]
readme = "README.md"

[tool.poetry.scripts]
max-portal-gun = "max_portal_gun.main:app"

[tool.poetry.dependencies]
python = "^3.9"
typer = {extras = ["all"], version = "^0.9.0"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
