from setuptools import setup

setup(name='kwakse_distributions',
      version='1.0',
      packages=['distributions'],
      author='Joshua Boateng',
      description='Gaussian and Binomial Distributions',
      zip_safe=False)
