[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "fastrix"
dynamic = ["version"]
authors = [
    {name = "GstoneSerenity"}
]
maintainers = [
    {name = "GstoneSerenity"}
]

[tool.setuptools.dynamic]
version = {attr = "fastrix.__version__"}