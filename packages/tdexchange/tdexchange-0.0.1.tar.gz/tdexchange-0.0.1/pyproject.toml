[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tdexchange"
version = "0.0.1"
authors = [
    { name = "Terry Qi", email="troppydash.developer@gmail.com" }
]
description = "Python Client for the TdExchange Server"
requires-python = ">=3.8"
dependencies = [
    "websocket-client"
]
license = { file = "LICENSE.txt" }

[tool.hatch.build.targets.sdist]
include = [
    "/src"
]