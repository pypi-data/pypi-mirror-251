# setup.py
from setuptools import setup

setup(
    name='niceterm',
    version='0.1.0',
    py_modules=['niceterm'],
    install_requires=[
        # List any dependencies your package may have
    ],
)
