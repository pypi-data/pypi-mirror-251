[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
version = "0.0.1"
name = "packaide_client"
requires-python = ">=3.7"
dependencies = [
    "pydantic",
    "requests"
]