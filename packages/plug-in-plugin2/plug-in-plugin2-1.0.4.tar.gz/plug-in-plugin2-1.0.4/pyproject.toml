[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["plug_in.plugin2"]
namespaces = true

[project]
name = "plug-in-plugin2"
version = "1.0.4"
description = "Plugin 2"
license = { text = "Plug-in Software License Agreement" }
