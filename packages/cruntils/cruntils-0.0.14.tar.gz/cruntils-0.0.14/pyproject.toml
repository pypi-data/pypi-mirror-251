[project]
name = "cruntils"
version = "0.0.14"
authors = [
  { name="Nambarc" },
]
description = "A collection of utilities."
readme = "README.md"
requires-python = ">=3.7"
dependencies = ["pywin32>=306", "pyserial>=3.5"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"