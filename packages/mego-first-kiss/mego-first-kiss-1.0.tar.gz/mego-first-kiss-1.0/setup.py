from setuptools import setup

setup(name='mego-first-kiss',
      version='1.0',
      description='Gaussian and Binomial distributions',
      packages=['mego-first-kiss'],
      author= 'Yousif Abozid',
      zip_safe=False)
