[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["plug-in.plugin1"]
namespaces = true

[project]
name = "plug-in-plugin1"
version = "1.0.3"
description = "Plugin 1"
license = { text = "Plug-in Software License Agreement" }
